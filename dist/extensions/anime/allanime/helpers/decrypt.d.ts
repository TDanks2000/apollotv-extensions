export declare class AllAnimeDecryptor {
    static oneDigitSymmetricXOR(password: number, target: string): string;
    static decryptAllAnime(password: string, target: string): string;
    private static hexStringToByteArray;
}
